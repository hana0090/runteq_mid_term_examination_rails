<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">RUNTEQ 中間試験</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <%= search_form_for @q, url: search_path do |f| %>
        <div class="d-flex">
          <%= f.search_field :title_or_body_cont, class: 'form-control me-2', placeholder: '投稿', id: 'search_field_of_post' %>
          <%= f.search_field :comments_body_cont, class: 'form-control me-2', placeholder: 'コメント', id: 'search_field_of_comment' %>
          <%= f.search_field :user_profile_name_cont_any, class: 'form-control me-2', placeholder: 'ユーザー', id: 'search_field_of_user' %>
          <%= f.submit class: 'btn btn-outline-success', value: "Search" %>
        </div>
      <% end %> 
      </form>
      <ul class="navbar-nav mb-2 mb-lg-0 ms-auto">
        <% if current_user.present? %>
          <li class="nav-item">
            <%= link_to '投稿一覧', posts_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to '投稿作成', new_post_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'ログアウト', logout_path, class: 'nav-link', method: :delete, data: { confirm: 'ログアウトします' } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'ユーザー登録', sign_up_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'ログイン', login_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
